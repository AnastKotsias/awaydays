# Copy to `.env`:  cp .env.example .env
#
# Only variables prefixed with VITE_ are exposed to browser code by Vite.
# That prefix is a safety rail: never put a secret in one, because everything
# here is bundled into the JavaScript your users download.

# Where the Express API is running
VITE_API_URL=http://localhost:4000
